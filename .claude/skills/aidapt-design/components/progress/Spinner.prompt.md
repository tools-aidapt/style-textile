Indeterminate ring spinner for inline loading. Pair with a label for longer waits.

```jsx
<Spinner size="sm" />
<Button variant="primary" loading>Saving…</Button>  /* Button has its own spinner */
```
Sizes xs–lg. Use `onDark` over Deep Flow / gradient surfaces (cyan arc). Respects reduced-motion.