Checkbox with inline label. Controlled (`checked`+`onChange`) or uncontrolled (`defaultChecked`).

```jsx
<Checkbox label="Use my saved glossary" defaultChecked />
```
Checked state fills Aidapt Teal with an Ink check. Supports `disabled` and `indeterminate` (set via ref).