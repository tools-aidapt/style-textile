On/off toggle for instant settings (auto-saves). For form submission prefer Checkbox.

```jsx
<Switch label="Auto-publish approved segments" defaultChecked />
```
Track turns Teal when on; the knob slides forward (left→right). Supports `disabled`.