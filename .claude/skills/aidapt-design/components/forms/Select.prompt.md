Native select styled with the brand chevron affordance. Pass `<option>` children.

```jsx
<Select defaultValue="ja"><option value="ja">Japanese</option><option value="de">German</option></Select>
```
Pass `invalid` for the error border. Use for short, known option sets; for long/searchable lists build a custom combobox.